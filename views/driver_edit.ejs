<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
            crossorigin="anonymous">

        <title>Редагування</title>
    </head>
    <body>
        <h1 class="text-center">Редагувати Водія</h1>
        <form action method="POST" class="text-center">
            <div class="mb-3">
                <label class="form-label">
                    Номер паспорту
                    <input type="text" placeholder="id" class="form-control"
                        id="passport_number" name="passport_number"
                        value= <%=Driver[0].passport_number %>
                    >
                    </label class="form-label">
                    <label>
                        ПІБ
                        <input type="text" class="form-control"
                            placeholder="Назва" id="pib"
                            name="pib"
                            style="width:300px"
                            value="<%= Driver[0].pib %>">
                    </label>
                    <label>
                        Дата народження
                        <input type="text" class="form-control"
                            placeholder="Назва" id="date_of_birth"
                            name="date_of_birth"
                            style="width:300px"
                            value="<%= Driver[0].date_of_birth %>">
                    </label>
                    <label>
                        Стать
                        <input type="text" class="form-control"
                            placeholder="Стать" id="gender"
                            name="gender"
                            value="<%= Driver[0].gender %>">
                    </label>
                    <label>
                        Стаж водіння
                        <input type="text" class="form-control"
                            placeholder="Стаж" id="driving_experience"
                            name="driving_experience"
                            value="<%= Driver[0].driving_experience %>">
                    </label>
                </div>
                <button type="submit" class="btn btn-primary"
                    formaction="/api/driver/put/<%= Driver[0].passport_number%>">
                    Редагувати</button>
            </form>
            <a class="btn btn-info" href="/api/driver">Повернутися</a>
        </body>
    </html>